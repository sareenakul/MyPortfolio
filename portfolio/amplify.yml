version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next # or dist, depending on your build output directory
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*