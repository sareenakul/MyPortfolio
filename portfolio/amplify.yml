version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
        - npm run export
  artifacts:
    baseDirectory: build # or dist, depending on your build output directory
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*